import { createClient } from '@supabase/supabase-js'; const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!u||!k){console.log('[admin-hub-smoke] skipped');process.exit(0)}const db=createClient(u,k);const{data:c,error:ce}=await db.from('countries').select('slug,status');if(ce)throw ce;for(const x of c??[])if(!['draft','published','closed'].includes(x.status))throw new Error(`Invalid country status: ${x.slug}`);const{data:a,error:ae}=await db.from('authors').select('id,slug,published');if(ae)throw ae;console.log(`[admin-hub-smoke] ${c?.length??0} countries; ${a?.length??0} authors OK`);
