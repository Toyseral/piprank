import { useEffect, useState } from 'react';
import { Eye, Pencil, Plus, Search } from 'lucide-react';
import type { Broker, ContentDocument, CountryBestFor, CountryPage } from '../../../lib/types';
import CountryGuides from '../CountryGuides';

type Props = { countries: CountryPage[]; brokers: Broker[]; countryBestFors?: CountryBestFor[]; contentDocs: ContentDocument[]; token: string; notify: (msg: string) => void; onNewCountry: () => void; onEditCountry: (country: CountryPage) => void; onEditBestFor?: (doc: ContentDocument) => void; onNewBestFor?: (countrySlug: string) => void; onEditCountryBestFor?: (page: CountryBestFor) => void; onNewCountryBestFor?: () => void };
function HubMetric({ label, value, sub }: { label: string; value: string; sub: string }) { return <div className="rounded-xl border border-line bg-paper p-3"><p className="text-[10px] font-bold uppercase tracking-widest text-slate-400">{label}</p><p className="mt-1 font-display text-xl font-bold text-ink-900">{value}</p><p className="mt-0.5 text-[10px] text-slate-400">{sub}</p></div>; }
function EntityPanel({ title, items }: { title: string; items: string[] }) { return <div className="rounded-2xl border border-line bg-white p-5"><h3 className="font-display text-lg font-bold text-ink-900">{title}</h3><div className="mt-3 space-y-2">{items.map((item)=><div key={item} className="flex items-center gap-2 rounded-lg bg-paper px-3 py-2 text-xs font-medium text-slate-600"><span className="h-1.5 w-1.5 shrink-0 rounded-full bg-emerald-500"/>{item}</div>)}</div></div>; }
export default function CountryHub({ countries, brokers, contentDocs, token, notify, onNewCountry, onEditCountry, onEditBestFor, onNewBestFor }: Props) { return <CountryHubInner countries={countries} brokers={brokers} contentDocs={contentDocs} token={token} notify={notify} onNewCountry={onNewCountry} onEditCountry={onEditCountry} onEditBestFor={onEditBestFor} onNewBestFor={onNewBestFor}/>; }
function CountryHubInner({ countries, brokers, contentDocs, token, notify, onNewCountry, onEditCountry, onEditBestFor, onNewBestFor }: Omit<Props,'countryBestFors'|'onEditCountryBestFor'|'onNewCountryBestFor'>) {
  const [query, setQuery] = useState(''); const [selectedSlug, setSelectedSlug] = useState(() => countries[0]?.slug ?? '');
  useEffect(() => { if (!selectedSlug && countries[0]) setSelectedSlug(countries[0].slug); }, [countries, selectedSlug]);
  const filtered = countries.filter((c) => `${c.name} ${c.slug}`.toLowerCase().includes(query.toLowerCase()));
  const selected = countries.find((c) => c.slug === selectedSlug) ?? filtered[0] ?? countries[0];
  const bestFor = selected ? contentDocs.filter((d) => d.content_type === 'country-best-for' && d.country_slug === selected.slug) : [];
  const docs = selected ? contentDocs.filter((d) => d.country_slug === selected.slug) : [];
  const publishedState = String((selected as any)?.publishing_state ?? ((selected as any)?.status ?? 'published'));
  return <div className="grid gap-5 lg:grid-cols-[300px_1fr]"><section className="rounded-2xl border border-line bg-white p-4"><div className="flex items-center justify-between"><h2 className="font-display text-lg font-bold text-ink-900">Countries</h2><button onClick={onNewCountry} className="rounded-lg bg-ink-950 px-3 py-1.5 text-xs font-bold text-white"><Plus size={13} className="inline"/> New</button></div><div className="mt-3 flex items-center gap-2 rounded-xl border border-line bg-paper px-3"><Search size={14} className="text-slate-400"/><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Search countries…" className="h-10 flex-1 bg-transparent text-sm outline-none"/></div><div className="mt-3 max-h-[560px] space-y-1 overflow-auto">{filtered.map((c)=><button key={c.id} onClick={()=>setSelectedSlug(c.slug)} className={`w-full rounded-xl px-3 py-2 text-left text-sm ${selected?.id===c.id?'bg-emerald-50 text-emerald-800':'hover:bg-paper'}`}><span className="font-bold">{c.flag} {c.name}</span><span className="block text-xs text-slate-400">/{c.slug}</span></button>)}</div></section>{selected&&<section className="space-y-5"><div className="rounded-2xl border border-line bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-widest text-emerald-700">Country Workspace</p><h2 className="font-display text-2xl font-bold text-ink-900">{selected.flag} {selected.name}</h2><p className="mt-1 text-sm text-slate-500">Country hub, broker coverage, canonical Best-For pages and guides.</p></div><button onClick={()=>onEditCountry(selected)} className="inline-flex items-center gap-1.5 rounded-xl bg-emerald-500 px-4 py-2 text-xs font-bold text-white"><Pencil size={14}/> Edit country hub</button></div><div className="mt-4 grid gap-3 sm:grid-cols-3"><HubMetric label="Publishing" value={publishedState} sub="draft · published · closed"/><HubMetric label="Best-For" value={String(bestFor.length)} sub="canonical documents"/><HubMetric label="Country content" value={String(docs.length)} sub="canonical documents"/></div></div><div className="grid gap-5 xl:grid-cols-2"><EntityPanel title="SEO QA" items={[selected.seo_title?'SEO title present':'Missing SEO title',selected.seo_description?'Meta description present':'Missing meta description',(selected.seo_intro?.length||0)>0?'Intro present':'Missing SEO intro',(selected.seo_sections?.length||0)>0?'Structured sections present':'Missing sections',(selected.seo_faqs?.length||0)>0?'FAQs present':'Missing FAQs']}/><EntityPanel title="Broker coverage" items={[`${selected.recommended.length} recommended brokers`,`${selected.unavailable.length} unavailable broker flags`,`${brokers.length} brokers in database`,'Use Broker Workspace for searchable eligibility states']}/></div><div className="rounded-2xl border border-line bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-2"><div><h3 className="font-display text-lg font-bold text-ink-900">Best-For pages</h3><p className="mt-1 text-xs text-slate-400">Canonical country documents; ranking presentation remains unchanged.</p></div><button onClick={()=>onNewBestFor?.(selected.slug)} className="inline-flex items-center gap-1.5 rounded-xl bg-emerald-500 px-3.5 py-1.5 text-xs font-bold text-white"><Plus size={13}/> Add new page</button></div><div className="mt-3 divide-y divide-line rounded-xl border border-line">{bestFor.map((doc)=><div key={doc.id} className="flex items-center justify-between px-4 py-3"><button onClick={()=>onEditBestFor?.(doc)} className="min-w-0 flex-1 text-left"><span className="block truncate text-sm font-bold text-ink-900">{doc.title||doc.slug||doc.content_key}</span><span className="text-xs text-slate-400">/{selected.slug}/{doc.slug} · {doc.published?'Published':'Draft'} · {doc.indexable?'Indexable':'Noindex'}</span></button><div className="flex shrink-0 items-center gap-1"><a href={`/${selected.slug}/${doc.slug}`} target="_blank" rel="noreferrer" className="rounded-lg p-2 text-slate-400 hover:bg-paper" title="Preview"><Eye size={14}/></a><button onClick={()=>onEditBestFor?.(doc)} className="rounded-lg p-2 text-slate-400 hover:bg-paper" title="Edit"><Pencil size={14}/></button></div></div>)}{!bestFor.length&&<p className="p-4 text-sm text-slate-400">No canonical country Best-For documents yet.</p>}</div></div><CountryGuides country={selected} countries={countries} brokers={brokers} token={token} notify={notify}/></section>}</div>;
}
