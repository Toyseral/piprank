import { useEffect,useState,type FormEvent } from 'react'; import type { Session } from '@supabase/supabase-js'; import supabase from '../lib/supabase'; import AdminHub from './AdminHub';
interface AdminSelfResponse { role?: string }
export default function AdminHubEntry(){const[s,setS]=useState<Session|null>(null),[role,setRole]=useState('checking'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');useEffect(()=>{supabase.auth.getSession().then(({data})=>setS(data.session));const{data}=supabase.auth.onAuthStateChange((_e,session)=>setS(session));return()=>data.subscription.unsubscribe()},[]);useEffect(()=>{if(!s){setRole('none');return;}setRole('checking');fetch('/api/admin-users?self=1',{headers:{Authorization:`Bearer ${s.access_token}`}}).then(r=>r.ok?r.json() as Promise<AdminSelfResponse>:Promise.resolve({})).then(d=>setRole(d.role||'none')).catch(()=>setRole('none'))},[s]);if(role==='checking')return <div className="flex min-h-screen items-center justify-center bg-paper">Loading admin…</div>;if(!s)return <div className="flex min-h-screen items-center justify-center bg-ink-950 px-4"><form onSubmit={async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');const{error:err}=await supabase.auth.signInWithPassword({email,password});if(err)setError(err.message);setBusy(false)}} className="w-full max-w-sm rounded-3xl bg-white p-8"><h1 className="font-display text-2xl font-bold">PipRank Admin</h1><p className="mt-1 text-sm text-slate-500">Sign in to the Admin Hub.</p><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="mt-5 h-11 w-full rounded-xl border border-line px-3 text-sm"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="mt-3 h-11 w-full rounded-xl border border-line px-3 text-sm"/>{error&&<p className="mt-2 text-sm text-rose-600">{error}</p>}<button disabled={busy} className="mt-4 w-full rounded-xl bg-ink-950 py-3 text-sm font-bold text-white">Sign in</button></form></div>;if(role==='none')return <div className="mx-auto max-w-md px-6 py-24 text-center"><h1 className="font-display text-2xl font-bold">No admin access</h1><p className="mt-2 text-sm text-slate-500">This account is not on the PipRank admin team.</p></div>;return <AdminHub session={s} role={role}/>}
