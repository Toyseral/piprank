import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import type { Broker, ContentDocument, FAQ } from '../lib/types';
import PageBlocksRenderer from './PageBlocksRenderer';
import PipRankComparisonTable from './PipRankComparisonTable';
import StructuredBrokerDataCard from './StructuredBrokerDataCard';
import PipRankVerdictCard from './PipRankVerdictCard';
import BestForQuickFacts from './BestForQuickFacts';
import Monogram from './Monogram';
import VisitButton from './VisitButton';
import { ButtonLink } from './Button';
import { isBlockShape } from '../lib/contentBlocks';
import { pipRankScore } from '../lib/score';
import { reviewerFor } from '../lib/team';

type Props = { document: ContentDocument; brokers: Broker[]; ranked: Broker[]; intentSlug: string; criteria: string[]; faqs: FAQ[]; countryName?: string; countrySlug?: string; localized?: boolean; locale?: string };
type ScopedBlock = { id: string; type: string; editorialSection?: string; [key: string]: unknown };
const SECTION_COPY_KEYS = { ranking: 'bestfor-copy:ranking', comparison: 'bestfor-copy:comparison', brokerAnalysis: 'bestfor-copy:brokerAnalysis' };
const DEFAULT_SECTION_COPY = { ranking: 'Explore the top eligible brokers for this category and jump directly to the full analysis of any broker that interests you.', comparison: 'Compare the eligible brokers side by side across the metrics that matter most for this category.', brokerAnalysis: "Review each broker's key facts, assessment, detailed analysis and PipRank verdict before you decide." };
const textFromHtml = (html: unknown) => String(html || '').replace(/<[^>]*>/g, ' ').replace(/&nbsp;/gi, ' ').replace(/\s+/g, ' ').trim();
const sectionCopyFromBlocks = (blocks: unknown, key: keyof typeof SECTION_COPY_KEYS) => {
  if (!isBlockShape(blocks)) return DEFAULT_SECTION_COPY[key];
  const block = (blocks as ScopedBlock[]).find((item) => item.id === SECTION_COPY_KEYS[key] && item.type === 'richtext');
  return textFromHtml(block?.html) || DEFAULT_SECTION_COPY[key];
};
const blocksFor = (blocks: unknown, section: string, brokerSlug?: string) => { if (!isBlockShape(blocks)) return [] as ScopedBlock[]; return (blocks as ScopedBlock[]).filter((block) => { if ((block.editorialSection || 'introduction') !== section) return false; const id = String(block.id || ''); if (id.startsWith('bestfor-copy:')) return false; return brokerSlug ? id.startsWith(`bestfor-broker:${brokerSlug}:`) : !id.startsWith('bestfor-broker:'); }); };
function EditorialZone({ blocks, brokers, intentSlug, countrySlug, section }: { blocks: unknown; brokers: Broker[]; intentSlug: string; countrySlug?: string; section: string }) { const scoped = blocksFor(blocks, section); return scoped.length ? <PageBlocksRenderer blocks={scoped as any} brokers={brokers} intent={intentSlug} countrySlug={countrySlug} className="piprank-rich-content" editorialSection={section} /> : null; }
function SectionIntro({ eyebrow, title, copy }: { eyebrow: string; title: string; copy?: string }) { return <div className="mb-8 max-w-4xl"><p className="text-[11px] font-bold uppercase tracking-[0.22em] text-emerald-700">{eyebrow}</p><h2 className="mt-2 font-display text-3xl font-bold tracking-tight text-ink-950 sm:text-4xl lg:text-[2.65rem]">{title}</h2>{copy && <p className="mt-3 max-w-3xl text-[15px] leading-7 text-slate-600 sm:text-base">{copy}</p>}</div>; }
function RankingList({ ranked }: { ranked: Broker[] }) { return <div className="-mx-5 overflow-hidden rounded-none border-y border-line bg-white shadow-soft sm:mx-0 sm:rounded-[24px] sm:border">{ranked.slice(0, 9).map((broker, index) => <a key={broker.id} href={`#bestfor-${broker.slug}`} className="group relative flex min-w-0 items-start gap-3 border-b border-line px-4 py-4 transition last:border-b-0 hover:bg-paper sm:items-center sm:gap-4 sm:px-6 sm:py-5"><div className="relative shrink-0 pt-1 sm:pt-0"><Monogram name={broker.name} color={broker.brand_color} logoUrl={broker.logo_url} size={48} className="rounded-xl" /><span className="absolute -left-1 -top-1 flex h-6 w-6 items-center justify-center rounded-full border-2 border-white bg-ink-950 font-display text-[10px] font-bold text-white shadow-sm sm:-left-1.5 sm:-top-1.5 sm:h-7 sm:w-7 sm:text-[11px]">{index + 1}</span></div><span className="min-w-0 flex-1 pr-1"><span className="block font-display text-[15px] font-bold leading-tight text-ink-950 group-hover:text-emerald-700 sm:text-base">{broker.name}</span>{broker.tagline && <span className="mt-1 block text-xs leading-5 text-slate-500 sm:text-sm">{broker.tagline}</span>}<span className="mt-2 flex max-w-full flex-nowrap gap-1.5 overflow-x-auto pb-0.5 sm:flex-wrap sm:overflow-visible">{(broker.best_for || []).slice(0, 2).map((item) => <span key={item} className="shrink-0 rounded-full bg-paper px-2 py-1 text-[9px] font-bold uppercase tracking-wide text-slate-500 sm:text-[10px]">{item.replace(/-/g, ' ')}</span>)}</span></span><span className="flex shrink-0 flex-col items-end gap-1 text-right"><span className="tnum font-display text-base font-bold text-emerald-700 sm:text-lg">{pipRankScore(broker)}<span className="ml-0.5 text-[9px] font-semibold text-slate-400 sm:text-[10px]">/100</span></span><span className="hidden text-[9px] font-bold uppercase tracking-wider text-slate-400 sm:block">PipRank score</span><span className="mt-1 flex h-8 w-8 items-center justify-center rounded-full border border-line bg-white text-slate-500 transition group-hover:border-emerald-300 group-hover:bg-emerald-50 group-hover:text-emerald-700" aria-hidden="true">→</span></span></a>)}</div>; }
function BrokerModule({ broker, rank, blocks, brokers, intentSlug, countrySlug }: { broker: Broker; rank: number; blocks: unknown; brokers: Broker[]; intentSlug: string; countrySlug?: string }) { const editorial = blocksFor(blocks, 'detailed_analysis', broker.slug); return <article id={`bestfor-${broker.slug}`} className="scroll-mt-28 -mx-5 overflow-hidden rounded-none border-y border-line bg-white shadow-soft-lg sm:mx-0 sm:rounded-[28px] sm:border"><header className="relative overflow-hidden bg-ink-950 px-4 py-4 text-white sm:px-7 sm:py-6 lg:px-8"><div className="absolute right-0 top-0 h-40 w-40 translate-x-1/3 -translate-y-1/3 rounded-full bg-emerald-400/10 blur-2xl" /><div className="relative flex min-w-0 flex-wrap items-center gap-3 sm:flex-nowrap sm:gap-4"><div className="relative shrink-0"><Monogram name={broker.name} color={broker.brand_color} logoUrl={broker.logo_url} size={50} className="shrink-0 rounded-2xl" /><span className="absolute -left-1 -top-1 flex h-6 w-6 items-center justify-center rounded-full border-2 border-ink-950 bg-white font-display text-[9px] font-bold text-ink-950 shadow-sm sm:-left-1.5 sm:-top-1.5 sm:h-7 sm:w-7 sm:text-[10px]">{rank}</span></div><div className="min-w-0 flex-1"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-emerald-300">Broker {rank}</p><h3 className="mt-1 font-display text-xl font-bold leading-tight tracking-tight sm:text-2xl">{broker.name}</h3><p className="mt-1 hidden truncate text-sm text-slate-300 sm:block">{broker.tagline}</p></div><div className="flex shrink-0 items-center gap-2 rounded-xl border border-white/10 bg-white/[0.07] px-3 py-2 sm:min-w-[96px] sm:flex-col sm:items-center sm:justify-center sm:rounded-2xl sm:px-4 sm:py-3"><p className="hidden text-[9px] font-bold uppercase tracking-wider text-slate-400 sm:block">PipRank score</p><p className="tnum whitespace-nowrap font-display text-xl font-bold text-white sm:text-2xl">{pipRankScore(broker)}<span className="text-[10px] font-semibold text-slate-400 sm:text-xs">/100</span></p></div>{broker.tagline && <p className="basis-full text-sm leading-6 text-slate-300 sm:hidden">{broker.tagline}</p>}</div></header><div className="space-y-7 p-5 sm:p-7 lg:p-8"><div className="-mx-5 sm:mx-0"><BestForQuickFacts broker={broker} /></div><div className="-mx-5 sm:mx-0 [&>section]:rounded-none [&>section]:border-y [&>section]:border-x-0 sm:[&>section]:rounded-3xl sm:[&>section]:border"><StructuredBrokerDataCard broker={broker} section="editorial" /></div>{editorial.length ? <section className="border-t border-line pt-7"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-emerald-700">Editorial analysis</p><h4 className="mt-2 font-display text-xl font-bold tracking-tight text-ink-950 sm:text-2xl">Why {broker.name} fits this category</h4><div className="mt-5 max-w-4xl"><PageBlocksRenderer blocks={editorial as any} brokers={brokers} intent={intentSlug} countrySlug={countrySlug} className="piprank-rich-content" editorialSection="detailed_analysis" /></div></section> : null}<PipRankVerdictCard broker={broker} headline={`Is ${broker.name} right for ${intentSlug.replace(/-/g, ' ')}?`} /></div></article>; }
export default function BestForTemplate({ document, brokers, ranked, intentSlug, criteria, faqs, countryName, countrySlug, localized, locale }: Props) { const settings = (document.settings ?? {}) as Record<string, any>; const fallbackAuthor = reviewerFor(`best-for-${countrySlug ?? 'global'}-${intentSlug}-${locale ?? ''}`); const [authorProfile, setAuthorProfile] = useState<ContentDocument | null>(null); useEffect(() => { const slug = String(settings.reviewed_by_slug || settings.author_slug || fallbackAuthor.slug).trim().toLowerCase(); fetch(`/api/content-documents?key=author:${encodeURIComponent(slug)}`).then(r => r.ok ? r.json() : null).then(a => setAuthorProfile(a?.published ? a : null)).catch(() => setAuthorProfile(null)); }, [document.content_key]); const authorSettings = (authorProfile?.settings ?? {}) as Record<string, any>; const author = authorProfile ? { slug: authorProfile.slug, penName: authorProfile.title || fallbackAuthor.penName, role: authorSettings.role || fallbackAuthor.role, bio: authorSettings.short_bio || authorProfile.excerpt || fallbackAuthor.bio, color: fallbackAuthor.color } : fallbackAuthor; const top9 = ranked.slice(0, 9); const top = top9[0]; const categoryLabel = intentSlug.replace(/-/g, ' '); const heroEyebrow = String(settings.label || (countryName ? `${countryName} · PipRank` : 'PipRank')); const comparisonFields = Array.isArray(settings.comparisonFields) ? settings.comparisonFields as any[] : undefined; const updated = new Date(document.updated_at).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' }); const rankingDescription = sectionCopyFromBlocks(document.blocks, 'ranking'); const comparisonDescription = sectionCopyFromBlocks(document.blocks, 'comparison'); const brokerAnalysisDescription = sectionCopyFromBlocks(document.blocks, 'brokerAnalysis'); return <main className="w-full overflow-x-clip bg-paper"><nav aria-label="Breadcrumb" className="w-full bg-ink-950 text-slate-400"><div className="mx-auto flex w-full max-w-[1440px] items-center gap-2 overflow-x-auto whitespace-nowrap px-4 py-3.5 text-xs sm:px-8 lg:px-12 xl:px-16"><Link to="/" className="transition hover:text-white">Home</Link><span className="text-slate-600">/</span>{countrySlug && <><Link to={`/${countrySlug}`} className="transition hover:text-white">{countryName || countrySlug}</Link><span className="text-slate-600">/</span></>}<span className="font-semibold text-white">{document.title}</span></div></nav><header className="w-full bg-ink-950 text-white"><div className="mx-auto grid w-full max-w-[1440px] gap-8 px-5 pb-12 pt-10 sm:px-8 sm:pb-16 sm:pt-14 lg:grid-cols-[minmax(0,1fr)_380px] lg:items-center lg:px-12 lg:pb-20 lg:pt-16 xl:px-16"><div className="min-w-0"><div className="flex flex-wrap items-center gap-3 text-[10px] font-bold uppercase tracking-[0.22em]"><span className="text-emerald-300">{heroEyebrow}</span><span className="h-1 w-1 rounded-full bg-slate-600" /><span className="text-slate-500">Updated {updated}</span></div><h1 className="mt-5 max-w-4xl font-display text-[clamp(2.5rem,6vw,5.5rem)] font-bold leading-[0.96] tracking-[-0.045em]">{document.title}</h1>{document.excerpt && <p className="mt-6 max-w-2xl text-[15px] leading-7 text-slate-300 sm:text-lg sm:leading-8">{document.excerpt}</p>}</div><div className="order-last rounded-none border border-white/10 bg-white/[0.045] p-5 shadow-2xl sm:order-none sm:rounded-[28px] sm:p-6"><p className="text-[9px] font-bold uppercase tracking-[0.18em] text-slate-500">Top match</p><div className="mt-4 flex items-center gap-4">{top && <Monogram name={top.name} color={top.brand_color} logoUrl={top.logo_url} size={58} className="shrink-0 rounded-2xl" />}<div className="min-w-0"><p className="truncate font-display text-xl font-bold text-white">{top?.name || '—'}</p>{top && <p className="mt-1 text-sm font-semibold text-emerald-300">{pipRankScore(top)}/100 PipRank score</p>}</div></div><p className="mt-5 border-t border-white/10 pt-4 text-sm leading-6 text-slate-400">The highest-ranked eligible broker for this category.</p></div>{top && <a href="#bestfor-brokers" className="order-last -mt-3 inline-flex w-full items-center justify-center rounded-none border border-white/15 bg-white/[0.06] px-4 py-3 text-sm font-semibold text-white transition hover:bg-white/10 sm:order-none sm:mt-0 sm:w-auto sm:justify-self-start sm:rounded-xl lg:col-start-1">Explore the rankings <span className="ml-2" aria-hidden="true">↓</span></a>}</div></header><section className="w-full bg-white"><div className="mx-auto w-full max-w-[1440px] px-5 py-10 sm:px-8 sm:py-14 lg:px-12 xl:px-16"><div className="max-w-4xl"><EditorialZone blocks={document.blocks} brokers={brokers} intentSlug={intentSlug} countrySlug={countrySlug} section="introduction" /></div></div></section>{top && <section className="w-full border-y border-line bg-paper"><div className="mx-auto w-full max-w-[1440px] px-5 py-12 sm:px-8 sm:py-16 lg:px-12 xl:px-16"><SectionIntro eyebrow="PipRank top broker" title={`${top.name} is our top match for ${categoryLabel}`} copy="The broker at the top of this shortlist based on eligibility, category fit and PipRank's ranking rules." /><article className="-mx-5 overflow-hidden rounded-none border-y border-line bg-ink-950 text-white shadow-soft-lg sm:mx-0 sm:rounded-[30px] sm:border"><div className="p-5 sm:p-8 lg:p-10"><div className="flex items-start gap-4 sm:gap-5"><Monogram name={top.name} color={top.brand_color} logoUrl={top.logo_url} size={72} className="shrink-0 rounded-2xl bg-white" /><div className="min-w-0"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-emerald-300">#1 for {categoryLabel}</p><h2 className="mt-1 truncate font-display text-3xl font-bold tracking-tight sm:text-4xl">{top.name}</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-300">{top.tagline}</p></div></div><div className="mt-7 grid gap-4 sm:grid-cols-[auto_1fr] sm:items-end"><div><p className="text-[9px] font-bold uppercase tracking-[0.18em] text-slate-500">PipRank score</p><p className="tnum mt-1 font-display text-5xl font-bold">{pipRankScore(top)}<span className="text-base font-semibold text-slate-500">/100</span></p></div><p className="max-w-xl text-sm leading-6 text-slate-400">See why {top.name} ranks first for this category, then compare the other eligible brokers below.</p></div><div className="mt-7 -mx-5 sm:mx-0"><BestForQuickFacts broker={top} /></div><div className="mt-6 flex flex-col gap-3 sm:flex-row"><VisitButton broker={top} className="w-full sm:w-auto" /><a href="#bestfor-brokers" className="inline-flex min-h-11 items-center justify-center rounded-xl border border-white/15 px-5 text-sm font-bold text-white transition hover:bg-white/10 sm:w-auto">See other brokers</a></div></div></article></div></section>}{top9.length > 0 && <section id="bestfor-brokers" className="w-full bg-white"><div className="mx-auto w-full max-w-[1440px] px-5 py-12 sm:px-8 sm:py-16 lg:px-12 xl:px-16"><SectionIntro eyebrow="Ranked shortlist" title={`Best 9 forex brokers for ${categoryLabel}`} copy={rankingDescription} /><div className="grid gap-8 lg:grid-cols-[minmax(0,1fr)_300px] lg:items-start"><RankingList ranked={top9} /><aside className="hidden rounded-[24px] bg-paper p-6 lg:block"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-slate-400">How to use this list</p><p className="mt-3 text-sm leading-6 text-slate-600">Start with the ranking, then jump to any broker below for the assessment, editorial analysis and verdict.</p><a href="#bestfor-detail" className="mt-5 inline-flex text-sm font-bold text-emerald-700">Jump to broker analysis →</a></aside></div></div></section>}{top9.length > 1 && <section className="w-full bg-paper"><div className="mx-auto w-full max-w-[1440px] px-5 py-12 sm:px-8 sm:py-16 lg:px-12 xl:px-16"><SectionIntro eyebrow="Quick comparison" title={`Compare the best ${top9.length} forex brokers for ${categoryLabel}`} copy={comparisonDescription} /><div className="-mx-5 sm:mx-0"><PipRankComparisonTable brokers={top9} fields={comparisonFields as any} title={`Compare the best ${top9.length} forex brokers for ${categoryLabel}`} showCta={false} /></div></div></section>}<section id="bestfor-detail" className="w-full bg-[#f7f7f5]"><div className="mx-auto w-full max-w-[1440px] px-5 py-14 sm:px-8 sm:py-20 lg:px-12 xl:px-16"><SectionIntro eyebrow="Detailed broker analysis" title={`Best forex broker for ${categoryLabel}`} copy={brokerAnalysisDescription} /><div className="space-y-8">{top9.map((broker, index) => <BrokerModule key={broker.id} broker={broker} rank={index + 1} blocks={document.blocks} brokers={brokers} intentSlug={intentSlug} countrySlug={countrySlug} />)}</div></div></section>{blocksFor(document.blocks, 'additional').length ? <section className="w-full bg-paper"><div className="mx-auto w-full max-w-[1440px] px-5 py-14 sm:px-8 lg:px-12 xl:px-16"><div className="max-w-4xl"><SectionIntro eyebrow="Additional content" title={`More on ${categoryLabel}`} /><EditorialZone blocks={document.blocks} brokers={brokers} intentSlug={intentSlug} countrySlug={countrySlug} section="additional" /></div></div></section> : null}{(blocksFor(document.blocks, 'why_these_brokers').length || blocksFor(document.blocks, 'who_its_for').length || blocksFor(document.blocks, 'who_its_not_for').length) ? <section className="w-full bg-white"><div className="mx-auto w-full max-w-[1440px] px-5 py-14 sm:px-8 lg:px-12 xl:px-16"><div className="max-w-4xl space-y-12"><SectionIntro eyebrow="Editorial content" title="More about these recommendations" /><EditorialZone blocks={document.blocks} brokers={brokers} intentSlug={intentSlug} countrySlug={countrySlug} section="why_these_brokers" /><EditorialZone blocks={document.blocks} brokers={brokers} intentSlug={intentSlug} countrySlug={countrySlug} section="who_its_for" /><EditorialZone blocks={document.blocks} brokers={brokers} intentSlug={intentSlug} countrySlug={countrySlug} section="who_its_not_for" /></div></div></section> : null}<SectionEditorial blocks={document.blocks} brokers={brokers} intentSlug={intentSlug} countrySlug={countrySlug} />{faqs.length > 0 && <section className="w-full bg-paper"><div className="mx-auto w-full max-w-[1440px] px-5 py-14 sm:px-8 sm:py-14 lg:px-12 xl:px-16"><div className="max-w-4xl"><SectionIntro eyebrow="FAQ" title="Frequently asked questions" /><div className="divide-y divide-line overflow-hidden rounded-[24px] border border-line bg-white">{faqs.map((faq) => <details key={faq.q} className="group px-5 py-5 sm:px-7"><summary className="flex cursor-pointer list-none items-center justify-between gap-6 font-display text-base font-bold text-ink-950 sm:text-lg"><span>{faq.q}</span><span className="text-xl font-normal text-slate-400 transition group-open:rotate-45">+</span></summary><p className="mt-3 max-w-3xl pr-8 text-sm leading-7 text-slate-600">{faq.a}</p></details>)}</div></div></div></section>}{criteria.length > 0 && <section className="w-full bg-white"><div className="mx-auto w-full max-w-[1440px] px-5 py-14 sm:px-8 lg:px-12 xl:px-16"><div className="max-w-4xl"><SectionIntro eyebrow="Criteria" title="What we considered" /><div className="grid gap-px overflow-hidden rounded-[24px] border border-line bg-line sm:grid-cols-2">{criteria.map((criterion) => <div key={criterion} className="bg-paper p-5 text-sm leading-6 text-slate-600 sm:p-6">{criterion}</div>)}</div></div></div></section>}<section className="w-full bg-ink-950 text-white"><div className="mx-auto w-full max-w-[1440px] px-5 py-14 sm:px-8 lg:px-12 xl:px-16"><div className="grid gap-8 lg:grid-cols-[1fr_auto] lg:items-center"><div><p className="text-[10px] font-bold uppercase tracking-[0.22em] text-emerald-300">Methodology</p><h2 className="mt-2 font-display text-3xl font-bold tracking-tight sm:text-4xl">How PipRank ranks brokers</h2><EditorialZone blocks={document.blocks} brokers={brokers} intentSlug={intentSlug} countrySlug={countrySlug} section="methodology" /><p className="mt-3 max-w-2xl text-[15px] leading-7 text-slate-300">PipRank ranks brokers using eligibility, category fit, pricing, trust and platform or feature data, with the final order reflecting the factors that matter most for the category.</p></div><Link to="/methodology" className="inline-flex items-center gap-1.5 text-sm font-bold text-white underline-offset-4 transition hover:text-emerald-300 hover:underline">Read more on how PipRank ranks brokers <span aria-hidden="true">→</span></Link></div></div></section><section className="w-full bg-white"><div className="mx-auto flex w-full max-w-[1440px] flex-col gap-6 px-5 py-14 sm:px-8 lg:flex-row lg:items-center lg:justify-between lg:px-12 xl:px-16"><div><p className="text-[10px] font-bold uppercase tracking-[0.22em] text-emerald-700">Personalized matching</p><h2 className="mt-2 font-display text-3xl font-bold tracking-tight text-ink-950 sm:text-4xl">Get matched with a broker that fits you</h2><p className="mt-3 max-w-2xl text-[15px] leading-7 text-slate-600">Tell us where you live, what you trade and what matters most. We’ll narrow down the brokers available to you.</p></div><ButtonLink variant="dark" size="md" to="/find-my-broker">Match Me With a Broker</ButtonLink></div></section><section className="w-full border-t border-line bg-paper"><div className="mx-auto w-full max-w-[1440px] px-5 py-12 sm:px-8 sm:py-14 lg:px-12 xl:px-16"><div className="flex max-w-4xl flex-col gap-5 sm:flex-row sm:items-start"><div className="flex h-14 w-14 shrink-0 items-center justify-center rounded-2xl text-sm font-black text-white" style={{ backgroundColor: author.color }}>{author.penName.split(' ').map((part) => part[0]).join('').slice(0, 2)}</div><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-emerald-700">Author & reviewer</p><h2 className="mt-1 font-display text-xl font-bold text-ink-950">Written & reviewed by {author.penName}</h2><p className="mt-1 text-sm font-semibold text-slate-600">{author.role}</p><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-600">{author.bio}</p><Link to={`/authors#${author.slug}`} className="mt-4 inline-flex text-xs font-bold text-emerald-700 hover:text-emerald-800">View author profile →</Link></div></div></div></section></main>; }
function SectionEditorial({ blocks, brokers, intentSlug, countrySlug }: { blocks: unknown; brokers: Broker[]; intentSlug: string; countrySlug?: string }) { const scoped = blocksFor(blocks, 'detailed_analysis'); return scoped.length ? <section className="w-full bg-white"><div className="mx-auto w-full max-w-[1440px] px-5 py-14 sm:px-8 lg:px-12 xl:px-16"><div className="max-w-4xl"><SectionIntro eyebrow="Additional analysis" title="More broker analysis" /><PageBlocksRenderer blocks={scoped as any} brokers={brokers} intent={intentSlug} countrySlug={countrySlug} className="piprank-rich-content" editorialSection="detailed_analysis" /></div></div></section> : null; }
