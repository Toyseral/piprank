import { useEffect, useState } from 'react';
import { ArrowDown, ArrowUp, Eye, GripVertical, Image as ImageIcon, Plus, Save, Table2, Trash2, Type, Quote, Link2, Database } from 'lucide-react';
import RichTextEditor from './RichTextEditor';
import type { Broker } from '../lib/types';
import { fetchBrokers } from '../lib/api';

export type StructuredBrokerSection = 'overview' | 'pricing' | 'trust' | 'platforms' | 'features' | 'editorial';
export type PageBlock={id:string;type:'richtext'|'heading'|'image'|'table'|'callout'|'divider'|'links'|'structured_broker_data';title?:string;html?:string;src?:string;alt?:string;rows?:string[][];tone?:'neutral'|'success'|'warning'|'dark';links?:{label:string;href:string}[];brokerId?:number;section?:StructuredBrokerSection};
type Props={value?:unknown[];onChange:(blocks:PageBlock[])=>void;onUploadImage?:(file:File)=>Promise<string>};
const uid=()=>`b_${Date.now()}_${Math.random().toString(36).slice(2,8)}`;
const norm=(v?:unknown[]):PageBlock[]=>Array.isArray(v)?v.map((b:any)=>({id:b.id||uid(),type:b.type||'richtext',...b})):[];
const esc=(v:string)=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c] as string));
const sectionLabel=(s:StructuredBrokerSection)=>({overview:'Overview',pricing:'Pricing & trading costs',trust:'Trust & regulation',platforms:'Trading platforms',features:'Features & payments',editorial:'Editorial highlights'}[s]);
function structuredBrokerHtml(block:PageBlock, brokers:Broker[]){const b=brokers.find(x=>x.id===Number(block.brokerId));if(!b)return '';const s=block.section||'overview';const rows: [string,string][] = s==='overview'?[['Broker',b.name],['Rating',`${b.rating}/5`],['Trust score',String(b.trust_score)],['Founded',String(b.founded||'')],['Headquarters',b.headquarters||'']]:s==='pricing'?[['Minimum deposit',String(b.min_deposit)],['EUR/USD spread',String(b.spread_eurusd)],['Commission',b.commission||''],['Maximum leverage',b.max_leverage||'']]:s==='trust'?[['Trust score',String(b.trust_score)],['Regulation',(b.regulations||[]).map(r=>r.body||r.country).filter(Boolean).join(', ')||'Not listed'],['Segregated funds',b.segregated?'Yes':'No'],['Negative balance protection',b.nbp?'Yes':'No'],['Risk warning',b.risk_warning||'']]:s==='platforms'?[['Platforms',(b.platforms||[]).join(', ')||'Not listed']]:s==='features'?[['Demo account',b.demo_account?'Yes':'No'],['Islamic account',b.islamic_account?'Yes':'No'],['Copy trading',b.copy_trading?'Yes':'No'],['Scalping',b.scalping?'Yes':'No'],['Hedging',b.hedging?'Yes':'No'],['Payment methods',(b.payments||[]).join(', ')||'Not listed']]:[['Best for',(b.best_for||[]).join(', ')||''],['Pros',(b.pros||[]).join(' • ')||''],['Cons',(b.cons||[]).join(' • ')||'']];return `<section class="piprank-structured-broker-data" data-broker-id="${Number(b.id)}" data-section="${esc(s)}"><h3>${esc(b.name)} — ${esc(sectionLabel(s))}</h3><div class="overflow-x-auto"><table><tbody>${rows.filter(([,v])=>v!==''&&v!=null).map(([k,v])=>`<tr><th>${esc(k)}</th><td>${esc(v)}</td></tr>`).join('')}</tbody></table></div></section>`}
export function blocksToHtml(blocks:PageBlock[], brokers:Broker[]=[]){const seen=new Map<string,number>();const slug=(s:string)=>{const base=s.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')||'section';const n=(seen.get(base)||0)+1;seen.set(base,n);return n>1?`${base}-${n}`:base};return blocks.map(b=>{if(b.type==='structured_broker_data')return structuredBrokerHtml(b,brokers);if(b.type==='heading')return `<h2 id="${slug(b.title||'section-heading')}">${esc(b.title||'Section heading')}</h2>`;if(b.type==='image')return `<figure><img src="${esc(b.src||'')}" alt="${esc(b.alt||'')}" loading="lazy" /><figcaption>${esc(b.alt||'')}</figcaption></figure>`;if(b.type==='table'){const r=b.rows||[['Feature','Details'],['','']];return `<div class="overflow-x-auto"><table><thead><tr>${r[0].map(c=>`<th>${esc(c)}</th>`).join('')}</tr></thead><tbody>${r.slice(1).map(x=>`<tr>${x.map(c=>`<td>${esc(c)}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}if(b.type==='callout')return `<aside class="piprank-callout piprank-callout-${b.tone||'neutral'}">${b.html||''}</aside>`;if(b.type==='links')return `<nav class="piprank-internal-links"><ul>${(b.links||[]).map(x=>`<li><a href="${esc(x.href)}">${esc(x.label)}</a></li>`).join('')}</ul></nav>`;if(b.type==='divider')return '<hr />';return b.html||''}).join('\n')}
export default function PageBuilder({value,onChange,onUploadImage}:Props){const [blocks,setBlocks]=useState(()=>norm(value));const [preview,setPreview]=useState(false);const [brokers,setBrokers]=useState<Broker[]>([]);useEffect(()=>{fetchBrokers().then(setBrokers).catch(()=>setBrokers([]))},[]);const emit=(x:PageBlock[])=>{setBlocks(x);onChange(x)};const add=(type:PageBlock['type'])=>{const b:PageBlock=type==='table'?{id:uid(),type,rows:[['Feature','Details'],['','']]}:type==='image'?{id:uid(),type,src:'',alt:''}:type==='heading'?{id:uid(),type,title:'New section'}:type==='callout'?{id:uid(),type,html:'<p>Add an important note.</p>',tone:'neutral'}:type==='links'?{id:uid(),type,links:[{label:'Related PipRank page',href:'/'}]}:type==='structured_broker_data'?{id:uid(),type,brokerId:brokers[0]?.id,section:'overview'}:{id:uid(),type,html:type==='divider'?'':'<p>Start writing this section…</p>'};emit([...blocks,b])};const move=(i:number,d:number)=>{const j=i+d;if(j<0||j>=blocks.length)return;const x=[...blocks];[x[i],x[j]]=[x[j],x[i]];emit(x)};const update=(i:number,p:Partial<PageBlock>)=>emit(blocks.map((b,n)=>n===i?{...b,...p}:b));
if(preview)return <div className="rounded-2xl border border-line bg-white"><div className="flex items-center justify-between border-b border-line p-3"><b className="text-sm">Page preview</b><button onClick={()=>setPreview(false)} className="rounded-xl border border-line px-3 py-2 text-xs font-bold">Back to editor</button></div><div className="piprank-rich-content p-6 sm:p-10" dangerouslySetInnerHTML={{__html:blocksToHtml(blocks,brokers)}}/></div>;
return <div className="rounded-2xl border border-line bg-slate-50 p-3 sm:p-4"><div className="sticky top-0 z-10 mb-4 flex flex-wrap items-center gap-2 rounded-xl border border-line bg-white p-2 shadow-sm"><span className="mr-1 inline-flex items-center gap-1 text-[10px] font-bold uppercase tracking-widest text-slate-400"><GripVertical size={13}/> Page Builder</span><button onClick={()=>add('heading')} className="tool"><Type size={14}/> Heading</button><button onClick={()=>add('richtext')} className="tool"><Type size={14}/> Text</button><button onClick={()=>add('image')} className="tool"><ImageIcon size={14}/> Image</button><button onClick={()=>add('table')} className="tool"><Table2 size={14}/> Table</button><button onClick={()=>add('callout')} className="tool"><Quote size={14}/> Callout</button><button onClick={()=>add('structured_broker_data')} className="tool"><Database size={14}/> Broker data</button><button onClick={()=>add('links')} className="tool"><Link2 size={14}/> Links</button><button onClick={()=>add('divider')} className="tool">— Divider</button><span className="flex-1"/><button onClick={()=>setPreview(true)} className="tool"><Eye size={14}/> Preview</button><span className="text-[10px] text-slate-400">{blocks.length} blocks</span></div><div className="space-y-3">{!blocks.length&&<button onClick={()=>add('richtext')} className="flex w-full items-center justify-center gap-2 rounded-2xl border-2 border-dashed border-slate-300 bg-white p-10 text-sm font-bold text-slate-500"><Plus size={18}/> Add your first section</button>}{blocks.map((b,i)=><div key={b.id} className="rounded-2xl border border-line bg-white"><div className="flex items-center gap-2 border-b border-line px-3 py-2"><span className="text-[10px] font-bold uppercase tracking-widest text-slate-400">{b.type}</span><span className="flex-1"/><button disabled={i===0} onClick={()=>move(i,-1)} className="iconbtn"><ArrowUp size={14}/></button><button disabled={i===blocks.length-1} onClick={()=>move(i,1)} className="iconbtn"><ArrowDown size={14}/></button><button onClick={()=>emit(blocks.filter((_,n)=>n!==i))} className="iconbtn text-rose-500"><Trash2 size={14}/></button></div><div className="p-3">{b.type==='structured_broker_data'&&<div className="grid gap-3 sm:grid-cols-2"><div><label className="label">Broker</label><select value={b.brokerId||''} onChange={e=>update(i,{brokerId:Number(e.target.value)})} className="input"><option value="">Select broker</option>{brokers.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></div><div><label className="label">Data section</label><select value={b.section||'overview'} onChange={e=>update(i,{section:e.target.value as StructuredBrokerSection})} className="input">{(['overview','pricing','trust','platforms','features','editorial'] as StructuredBrokerSection[]).map(x=><option key={x} value={x}>{sectionLabel(x)}</option>)}</select></div><p className="text-xs text-slate-500 sm:col-span-2">Values are read from the Broker Workspace when the page is rendered. They are not copied into this block.</p></div>}{b.type==='heading'&&<input value={b.title||''} onChange={e=>update(i,{title:e.target.value})} className="w-full rounded-xl border border-line px-3 py-2.5 font-display text-xl font-bold"/>}{b.type==='richtext'&&<RichTextEditor value={b.html||''} onChange={html=>update(i,{html})} onUploadImage={onUploadImage}/>} {b.type==='image'&&<div className="grid gap-3 sm:grid-cols-2"><div><label className="label">Image URL</label><input value={b.src||''} onChange={e=>update(i,{src:e.target.value})} className="input"/><label className="label mt-3">Alt text</label><input value={b.alt||''} onChange={e=>update(i,{alt:e.target.value})} className="input"/>{onUploadImage&&<label className="mt-3 inline-flex cursor-pointer rounded-xl bg-ink-950 px-3 py-2 text-xs font-bold text-white">Upload image<input type="file" accept="image/*" className="hidden" onChange={async e=>{const f=e.target.files?.[0];if(!f)return;const url=await onUploadImage(f);update(i,{src:url});e.currentTarget.value=''}}/></label>}</div><div className="flex min-h-40 items-center justify-center rounded-xl bg-slate-100">{b.src?<img src={b.src} alt={b.alt||''} className="max-h-64 rounded-xl object-contain"/>:<span className="text-xs text-slate-400">Image preview</span>}</div></div>}{b.type==='table'&&<TableBlock block={b} onChange={p=>update(i,p)}/>} {b.type==='links'&&<div><p className="mb-2 text-xs font-bold text-slate-500">Internal links</p>{(b.links||[]).map((x,n)=><div key={n} className="mb-2 grid grid-cols-[1fr_1fr_auto] gap-2"><input value={x.label} onChange={e=>update(i,{links:(b.links||[]).map((y,j)=>j===n?{...y,label:e.target.value}:y)})} className="input" placeholder="Link label"/><input value={x.href} onChange={e=>update(i,{links:(b.links||[]).map((y,j)=>j===n?{...y,href:e.target.value}:y)})} className="input" placeholder="/ghana/gold-forex-brokers"/><button onClick={()=>update(i,{links:(b.links||[]).filter((_,j)=>j!==n)})} className="iconbtn text-rose-500"><Trash2 size={14}/></button></div>)}<button onClick={()=>update(i,{links:[...(b.links||[]),{label:'New link',href:'/'}]})} className="mt-1 inline-flex items-center gap-1 rounded-lg border border-line px-2.5 py-1.5 text-xs font-bold"><Plus size={12}/> Add link</button></div>}{b.type==='callout'&&<div><select value={b.tone||'neutral'} onChange={e=>update(i,{tone:e.target.value as any})} className="mb-2 rounded-lg border border-line px-2 py-1 text-xs"><option>neutral</option><option>success</option><option>warning</option><option>dark</option></select><RichTextEditor value={b.html||''} onChange={html=>update(i,{html})}/></div>}{b.type==='divider'&&<hr/>}</div></div>)}</div><div className="mt-4 flex items-center justify-between rounded-xl bg-ink-950 px-3 py-2 text-white"><span className="text-[11px] text-slate-400">Reorder sections, add rich media, then save the page.</span><button onClick={()=>onChange(blocks)} className="inline-flex items-center gap-1.5 rounded-lg bg-emerald-400 px-3 py-1.5 text-xs font-bold"><Save size={13}/> Save builder state</button></div><style>{`.tool{display:inline-flex;align-items:center;gap:.3rem;border:1px solid #e2e8f0;border-radius:.65rem;padding:.45rem .65rem;font-size:.7rem;font-weight:700;background:white}.iconbtn{padding:.35rem;border-radius:.5rem;color:#94a3b8}.iconbtn:disabled{opacity:.25}.label{display:block;font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.08em;color:#94a3b8;margin-bottom:.35rem}.input{width:100%;border:1px solid #e2e8f0;border-radius:.75rem;padding:.6rem .75rem;font-size:.8rem}`}</style></div>}
function TableBlock({block,onChange}:{block:PageBlock;onChange:(p:Partial<PageBlock>)=>void}){const rows=block.rows||[['Feature','Details'],['','']];const set=(r:number,c:number,v:string)=>{const n=rows.map(x=>[...x]);n[r][c]=v;onChange({rows:n})};return <div className="overflow-x-auto"><table className="w-full border-collapse"><tbody>{rows.map((row,r)=><tr key={r}>{row.map((cell,c)=><td key={c} className="border border-line p-1"><input value={cell} onChange={e=>set(r,c,e.target.value)} className={`w-full bg-transparent px-2 py-2 text-xs ${r===0?'font-bold':''}`}/></td>)}</tr>)}</tbody></table><button onClick={()=>onChange({rows:[...rows,Array(rows[0]?.length||2).fill('')]})} className="mt-2 inline-flex items-center gap-1 rounded-lg border border-line px-2.5 py-1.5 text-xs font-bold"><Plus size={12}/> Add row</button></div>}
