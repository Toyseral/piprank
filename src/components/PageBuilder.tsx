import { useState } from 'react';
import { ArrowDown, ArrowUp, Eye, GripVertical, Plus, Save, Trash2 } from 'lucide-react';
import RichTextEditor from './RichTextEditor';
import type { Broker } from '../lib/types';

export type BrokerCardVariant = 'default' | 'compact' | 'featured';
export type BrokerCtaVariant = 'primary' | 'dark' | 'soft';
export type ComparisonField = 'rating' | 'trust_score' | 'min_deposit' | 'spread_eurusd' | 'commission' | 'max_leverage' | 'platforms' | 'payments' | 'regulations';
export type PageBlock = { id:string; type:'richtext'|'heading'|'image'|'table'|'callout'|'divider'|'links'|'broker_card'|'broker_grid'|'comparison_table'|'broker_cta'; title?:string; html?:string; src?:string; alt?:string; rows?:string[][]; tone?:'neutral'|'success'|'warning'|'dark'; links?:{label:string;href:string}[]; brokerId?:number; brokerIds?:number[]; variant?:BrokerCardVariant|BrokerCtaVariant; headline?:string; description?:string; buttonLabel?:string; fields?:ComparisonField[] };
type Props={value?:unknown[];onChange:(blocks:PageBlock[])=>void;onUploadImage?:(file:File)=>Promise<string>;brokers?:Broker[]};
const uid=()=>`b_${Date.now()}_${Math.random().toString(36).slice(2,8)}`;
const norm=(v?:unknown[]):PageBlock[]=>Array.isArray(v)?v.map((b:any)=>({id:b.id||uid(),type:b.type||'richtext',...b})):[];
const esc=(v:string)=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c] as string));
const safeHref=(v:string)=>esc(v).replace(/javascript\s*:/gi,'');
const byId=(brokers:Broker[],id?:number)=>brokers.find(b=>Number(b.id)===Number(id));
const fieldLabel=(f:ComparisonField)=>({rating:'Rating',trust_score:'Trust score',min_deposit:'Min deposit',spread_eurusd:'EUR/USD spread',commission:'Commission',max_leverage:'Max leverage',platforms:'Platforms',payments:'Payments',regulations:'Regulation'} as Record<ComparisonField,string>)[f];
const fieldValue=(b:Broker,f:ComparisonField)=>f==='rating'?`${Number(b.rating||0).toFixed(1)}/5`:f==='trust_score'?`${Number(b.trust_score||0)}/100`:f==='min_deposit'?`$${b.min_deposit??0}`:f==='spread_eurusd'?`${b.spread_eurusd??'—'} pips`:f==='platforms'?(b.platforms||[]).join(', ')||'—':f==='payments'?(b.payments||[]).join(', ')||'—':f==='regulations'?(b.regulations||[]).map(r=>r.body||r.country).filter(Boolean).join(', ')||'—':String((b as any)[f]??'—');
function cardHtml(b:Broker,variant:BrokerCardVariant='default'){const logo=b.logo_url?`<img src="${safeHref(b.logo_url)}" alt="${esc(b.name)} logo" loading="lazy" />`:`<span class="piprank-broker-monogram">${esc(b.name.slice(0,1))}</span>`;const target=b.affiliate_url||b.website;return `<article class="piprank-broker-card piprank-broker-card-${variant}"><div class="piprank-broker-card-head"><div class="piprank-broker-brand">${logo}</div><div><h3>${esc(b.name)}</h3><p>${esc(b.tagline||'')}</p></div><strong>${Number(b.rating||0).toFixed(1)}/5</strong></div><div class="piprank-broker-card-stats"><span><b>$${esc(String(b.min_deposit??0))}</b><small>Min deposit</small></span><span><b>${esc(String(b.spread_eurusd??'—'))} pips</b><small>EUR/USD spread</small></span><span><b>${esc(String(b.max_leverage??'—'))}</b><small>Max leverage</small></span></div><a class="piprank-broker-card-cta" href="${safeHref(target)}" rel="nofollow sponsored">Visit ${esc(b.name)}</a></article>`}
export function blocksToHtml(blocks:PageBlock[],brokers:Broker[]=[]){const seen=new Map<string,number>();const headingId=(s:string)=>{const base=s.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')||'section';const n=(seen.get(base)||0)+1;seen.set(base,n);return n>1?`${base}-${n}`:base};return blocks.map(b=>{if(b.type==='heading')return `<h2 id="${headingId(b.title||'section-heading')}">${esc(b.title||'Section heading')}</h2>`;if(b.type==='image')return `<figure><img src="${safeHref(b.src||'')}" alt="${esc(b.alt||'')}" loading="lazy" /><figcaption>${esc(b.alt||'')}</figcaption></figure>`;if(b.type==='table'){const r=b.rows||[['Feature','Details'],['','']];return `<div class="overflow-x-auto"><table><thead><tr>${r[0].map(c=>`<th>${esc(c)}</th>`).join('')}</tr></thead><tbody>${r.slice(1).map(x=>`<tr>${x.map(c=>`<td>${esc(c)}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}if(b.type==='callout')return `<aside class="piprank-callout piprank-callout-${b.tone||'neutral'}">${b.html||''}</aside>`;if(b.type==='links')return `<nav class="piprank-internal-links"><ul>${(b.links||[]).map(x=>`<li><a href="${safeHref(x.href)}">${esc(x.label)}</a></li>`).join('')}</ul></nav>`;if(b.type==='divider')return '<hr />';if(b.type==='broker_card'){const x=byId(brokers,b.brokerId);return x?cardHtml(x,(b.variant as BrokerCardVariant)||'default'):''}if(b.type==='broker_grid')return `<div class="piprank-broker-grid">${(b.brokerIds||[]).map(id=>byId(brokers,id)).filter(Boolean).map(x=>cardHtml(x!,(b.variant as BrokerCardVariant)||'default')).join('')}</div>`;if(b.type==='comparison_table'){const selected=(b.brokerIds||[]).map(id=>byId(brokers,id)).filter(Boolean) as Broker[];const fields:ComparisonField[]=b.fields?.length?b.fields:['rating','min_deposit','spread_eurusd','commission','max_leverage'];return `<div class="overflow-x-auto piprank-comparison"><table><thead><tr><th>Feature</th>${selected.map(x=>`<th>${esc(x.name)}</th>`).join('')}</tr></thead><tbody>${fields.map(f=>`<tr><th>${esc(fieldLabel(f))}</th>${selected.map(x=>`<td>${esc(fieldValue(x,f))}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}if(b.type==='broker_cta'){const x=byId(brokers,b.brokerId);if(!x)return '';const target=x.affiliate_url||x.website;return `<aside class="piprank-broker-cta piprank-broker-cta-${b.variant||'primary'}"><div><strong>${esc(b.headline||`Ready to try ${x.name}?`)}</strong><p>${esc(b.description||x.tagline||'')}</p></div><a href="${safeHref(target)}" rel="nofollow sponsored">${esc(b.buttonLabel||`Visit ${x.name}`)}</a></aside>`}return b.html||''}).join('\n')}
const comparisonFields:ComparisonField[]=['rating','trust_score','min_deposit','spread_eurusd','commission','max_leverage','platforms','payments','regulations'];
export default function PageBuilder({value,onChange,onUploadImage,brokers=[]}:Props){const[blocks,setBlocks]=useState(()=>norm(value));const[preview,setPreview]=useState(false);const emit=(x:PageBlock[])=>{setBlocks(x);onChange(x)};const add=(type:PageBlock['type'])=>{const b:PageBlock=type==='table'?{id:uid(),type,rows:[['Feature','Details'],['','']]}:type==='image'?{id:uid(),type,src:'',alt:''}:type==='heading'?{id:uid(),type,title:'New section'}:type==='callout'?{id:uid(),type,html:'<p>Add an important note.</p>',tone:'neutral'}:type==='links'?{id:uid(),type,links:[{label:'Related PipRank page',href:'/'}]}:type==='broker_card'?{id:uid(),type,brokerId:brokers[0]?.id,variant:'default'}:type==='broker_grid'?{id:uid(),type,brokerIds:brokers.slice(0,3).map(x=>x.id),variant:'default'}:type==='comparison_table'?{id:uid(),type,brokerIds:brokers.slice(0,3).map(x=>x.id),fields:['rating','min_deposit','spread_eurusd','commission','max_leverage']}:type==='broker_cta'?{id:uid(),type,brokerId:brokers[0]?.id,variant:'primary'}:{id:uid(),type,html:type==='divider'?'':'<p>Start writing this section…</p>'};emit([...blocks,b])};const move=(i:number,d:number)=>{const j=i+d;if(j<0||j>=blocks.length)return;const x=[...blocks];[x[i],x[j]]=[x[j],x[i]];emit(x)};const update=(i:number,p:Partial<PageBlock>)=>emit(blocks.map((b,n)=>n===i?{...b,...p}:b));if(preview)return <div className="rounded-2xl border border-line bg-white"><div className="flex items-center justify-between border-b border-line p-3"><b className="text-sm">Page preview</b><button onClick={()=>setPreview(false)} className="rounded-xl border border-line px-3 py-2 text-xs font-bold">Back to editor</button></div><div className="piprank-rich-content p-6 sm:p-10" dangerouslySetInnerHTML={{__html:blocksToHtml(blocks,brokers)}}/></div>;
return <div className="rounded-2xl border border-line bg-slate-50 p-3 sm:p-4"><div className="sticky top-0 z-10 mb-4 flex flex-wrap items-center gap-2 rounded-xl border border-line bg-white p-2 shadow-sm"><span className="mr-1 inline-flex items-center gap-1 text-[10px] font-bold uppercase tracking-widest text-slate-400"><GripVertical size={13}/> Page Builder</span>{([['heading','Heading'],['richtext','Text'],['image','Image'],['table','Table'],['callout','Callout'],['broker_card','Broker Card'],['broker_grid','Broker Grid'],['comparison_table','Compare'],['broker_cta','Broker CTA'],['links','Links'],['divider','Divider']] as [PageBlock['type'],string][]).map(([t,l])=><button key={t} onClick={()=>add(t)} className="tool">{l}</button>)}<span className="flex-1"/><button onClick={()=>setPreview(true)} className="tool"><Eye size={14}/> Preview</button><span className="text-[10px] text-slate-400">{blocks.length} blocks</span></div><div className="space-y-3">{!blocks.length&&<button onClick={()=>add('richtext')} className="flex w-full items-center justify-center gap-2 rounded-2xl border-2 border-dashed border-slate-300 bg-white p-10 text-sm font-bold text-slate-500"><Plus size={18}/> Add your first section</button>}{blocks.map((b,i)=><div key={b.id} className="rounded-2xl border border-line bg-white"><div className="flex items-center gap-2 border-b border-line px-3 py-2"><span className="text-[10px] font-bold uppercase tracking-widest text-slate-400">{b.type}</span><span className="flex-1"/><button disabled={i===0} onClick={()=>move(i,-1)} className="iconbtn"><ArrowUp size={14}/></button><button disabled={i===blocks.length-1} onClick={()=>move(i,1)} className="iconbtn"><ArrowDown size={14}/></button><button onClick={()=>emit(blocks.filter((_,n)=>n!==i))} className="iconbtn text-rose-500"><Trash2 size={14}/></button></div><div className="p-3">{b.type==='heading'&&<input value={b.title||''} onChange={e=>update(i,{title:e.target.value})} className="w-full rounded-xl border border-line px-3 py-2.5 font-display text-xl font-bold"/>}{b.type==='richtext'&&<RichTextEditor value={b.html||''} onChange={html=>update(i,{html})} onUploadImage={onUploadImage}/>} {b.type==='image'&&<div className="grid gap-3 sm:grid-cols-2"><div><label className="label">Image URL</label><input value={b.src||''} onChange={e=>update(i,{src:e.target.value})} className="input"/><label className="label mt-3">Alt text</label><input value={b.alt||''} onChange={e=>update(i,{alt:e.target.value})} className="input"/>{onUploadImage&&<label className="mt-3 inline-flex cursor-pointer rounded-xl bg-ink-950 px-3 py-2 text-xs font-bold text-white">Upload image<input type="file" accept="image/*" className="hidden" onChange={async e=>{const f=e.target.files?.[0];if(!f)return;const url=await onUploadImage(f);update(i,{src:url});e.currentTarget.value=''}}/></label>}</div><div className="flex min-h-40 items-center justify-center rounded-xl bg-slate-100">{b.src?<img src={b.src} alt={b.alt||''} className="max-h-64 rounded-xl object-contain"/>:<span className="text-xs text-slate-400">Image preview</span>}</div></div>}{b.type==='table'&&<TableBlock block={b} onChange={p=>update(i,p)}/>} {b.type==='links'&&<LinksBlock block={b} onChange={p=>update(i,p)}/>} {b.type==='callout'&&<div><select value={b.tone||'neutral'} onChange={e=>update(i,{tone:e.target.value as any})} className="mb-2 rounded-lg border border-line px-2 py-1 text-xs"><option>neutral</option><option>success</option><option>warning</option><option>dark</option></select><RichTextEditor value={b.html||''} onChange={html=>update(i,{html})}/></div>}{(b.type==='broker_card'||b.type==='broker_cta')&&<BrokerSingleBlock block={b} brokers={brokers} onChange={p=>update(i,p)}/>} {b.type==='broker_grid'&&<BrokerMultiBlock block={b} brokers={brokers} onChange={p=>update(i,p)}/>} {b.type==='comparison_table'&&<ComparisonBlock block={b} brokers={brokers} onChange={p=>update(i,p)}/>} {b.type==='divider'&&<hr/>}</div></div>)}</div><div className="mt-4 flex items-center justify-between rounded-xl bg-ink-950 px-3 py-2 text-white"><span className="text-[11px] text-slate-400">Broker facts are referenced read-only from the broker database.</span><button onClick={()=>onChange(blocks)} className="inline-flex items-center gap-1.5 rounded-lg bg-emerald-400 px-3 py-1.5 text-xs font-bold text-ink-950"><Save size={13}/> Save builder state</button></div><style>{`.tool{display:inline-flex;align-items:center;gap:.3rem;border:1px solid #e2e8f0;border-radius:.65rem;padding:.45rem .65rem;font-size:.7rem;font-weight:700;background:white}.iconbtn{padding:.35rem;border-radius:.5rem;color:#94a3b8}.iconbtn:disabled{opacity:.25}.label{display:block;font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.08em;color:#94a3b8;margin-bottom:.35rem}.input{width:100%;border:1px solid #e2e8f0;border-radius:.75rem;padding:.6rem .75rem;font-size:.8rem}`}</style></div>}
function BrokerSingleBlock({block,brokers,onChange}:{block:PageBlock;brokers:Broker[];onChange:(p:Partial<PageBlock>)=>void}){return <div className="space-y-3"><label><span className="label">Broker</span><select value={block.brokerId||''} onChange={e=>onChange({brokerId:Number(e.target.value)||undefined})} className="input"><option value="">Select broker</option>{brokers.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select></label><label><span className="label">Variant</span><select value={block.variant||'primary'} onChange={e=>onChange({variant:e.target.value as any})} className="input">{block.type==='broker_card'?<><option value="default">Default</option><option value="compact">Compact</option><option value="featured">Featured</option></>:<><option value="primary">Primary</option><option value="dark">Dark</option><option value="soft">Soft</option></>}</select></label>{block.type==='broker_cta'&&<div className="grid gap-3 sm:grid-cols-2"><label><span className="label">Headline</span><input value={block.headline||''} onChange={e=>onChange({headline:e.target.value})} className="input"/></label><label><span className="label">Button label</span><input value={block.buttonLabel||''} onChange={e=>onChange({buttonLabel:e.target.value})} className="input"/></label><label className="sm:col-span-2"><span className="label">Description</span><textarea value={block.description||''} onChange={e=>onChange({description:e.target.value})} className="input" rows={2}/></label></div>}</div>}
function BrokerMultiBlock({block,brokers,onChange}:{block:PageBlock;brokers:Broker[];onChange:(p:Partial<PageBlock>)=>void}){const ids=block.brokerIds||[];return <div><span className="label">Select brokers</span><div className="grid gap-2 sm:grid-cols-2">{brokers.map(b=><label key={b.id} className="flex items-center gap-2 rounded-xl border border-line p-3 text-sm"><input type="checkbox" checked={ids.includes(b.id)} onChange={()=>onChange({brokerIds:ids.includes(b.id)?ids.filter(x=>x!==b.id):[...ids,b.id]})}/><span className="font-semibold">{b.name}</span><span className="ml-auto text-xs text-slate-400">{Number(b.rating||0).toFixed(1)}</span></label>)}</div><label className="mt-3 block"><span className="label">Card variant</span><select value={block.variant||'default'} onChange={e=>onChange({variant:e.target.value as any})} className="input"><option value="default">Default</option><option value="compact">Compact</option><option value="featured">Featured</option></select></label></div>}
function ComparisonBlock({block,brokers,onChange}:{block:PageBlock;brokers:Broker[];onChange:(p:Partial<PageBlock>)=>void}){const ids=block.brokerIds||[];const fields:ComparisonField[]=block.fields?.length?block.fields:['rating','min_deposit','spread_eurusd','commission','max_leverage'];return <div><span className="label">Brokers</span><div className="grid gap-2 sm:grid-cols-2">{brokers.map(b=><label key={b.id} className="flex items-center gap-2 rounded-xl border border-line p-3 text-sm"><input type="checkbox" checked={ids.includes(b.id)} onChange={()=>onChange({brokerIds:ids.includes(b.id)?ids.filter(x=>x!==b.id):[...ids,b.id]})}/><span className="font-semibold">{b.name}</span></label>)}</div><span className="label mt-4">Comparison fields</span><div className="grid gap-2 sm:grid-cols-3">{comparisonFields.map(f=><label key={f} className="flex items-center gap-2 rounded-lg border border-line p-2 text-xs"><input type="checkbox" checked={fields.includes(f)} onChange={()=>onChange({fields:fields.includes(f)?fields.filter(x=>x!==f):[...fields,f]})}/>{fieldLabel(f)}</label>)}</div></div>}
function LinksBlock({block,onChange}:{block:PageBlock;onChange:(p:Partial<PageBlock>)=>void}){const links=block.links||[];return <div><p className="mb-2 text-xs font-bold text-slate-500">Internal links</p>{links.map((x,n)=><div key={n} className="mb-2 grid grid-cols-[1fr_1fr_auto] gap-2"><input value={x.label} onChange={e=>onChange({links:links.map((y,j)=>j===n?{...y,label:e.target.value}:y)})} className="input"/><input value={x.href} onChange={e=>onChange({links:links.map((y,j)=>j===n?{...y,href:e.target.value}:y)})} className="input"/><button onClick={()=>onChange({links:links.filter((_,j)=>j!==n)})} className="iconbtn text-rose-500"><Trash2 size={14}/></button></div>)}<button onClick={()=>onChange({links:[...links,{label:'New link',href:'/'}]})} className="mt-1 inline-flex items-center gap-1 rounded-lg border border-line px-2.5 py-1.5 text-xs font-bold"><Plus size={12}/> Add link</button></div>}
function TableBlock({block,onChange}:{block:PageBlock;onChange:(p:Partial<PageBlock>)=>void}){const rows=block.rows||[['Feature','Details'],['','']];const set=(r:number,c:number,v:string)=>{const n=rows.map(x=>[...x]);n[r][c]=v;onChange({rows:n})};return <div className="overflow-x-auto"><table className="w-full border-collapse"><tbody>{rows.map((row,r)=><tr key={r}>{row.map((cell,c)=><td key={c} className="border border-line p-1"><input value={cell} onChange={e=>set(r,c,e.target.value)} className={`w-full bg-transparent px-2 py-2 text-xs ${r===0?'font-bold':''}`}/></td>)}</tr>)}</tbody></table><button onClick={()=>onChange({rows:[...rows,Array(rows[0]?.length||2).fill('')]})} className="mt-2 inline-flex items-center gap-1 rounded-lg border border-line px-2.5 py-1.5 text-xs font-bold"><Plus size={12}/> Add row</button></div>}
