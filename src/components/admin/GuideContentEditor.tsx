import { useEffect, useState } from 'react';
import type { Broker, ContentDocument } from '../../lib/types';
import PageBuilder, { blocksToHtml, type PageBlock } from '../PageBuilder';

export default function GuideContentEditor({ document, brokers, token, onClose, onSave }: { document: ContentDocument|null; brokers: Broker[]; token: string; onClose:()=>void; onSave:(doc:ContentDocument,isNew:boolean)=>Promise<void> }) {
  const [form,setForm]=useState<any>(()=>document?{...document}:{id:0,content_key:'country-guide:new',content_type:'country-guide',country_slug:null,topic_slug:null,slug:'',title:'',excerpt:'',html:'',blocks:[],seo_title:null,seo_description:null,indexable:true,published:false,settings:{}});
  const [blocks,setBlocks]=useState<PageBlock[]>(()=>Array.isArray(document?.blocks)?document!.blocks as PageBlock[]:[]);
  const [busy,setBusy]=useState(false); const [error,setError]=useState('');
  useEffect(()=>{if(!document)return;setForm({...document});setBlocks(Array.isArray(document.blocks)?document.blocks as PageBlock[]:document.html?[{id:'legacy',type:'richtext',html:document.html}]:[])},[document?.id]);
  const uploadImage=async(file:File)=>{const reader=new FileReader();const data=await new Promise<string>((resolve,reject)=>{reader.onload=()=>resolve(String(reader.result));reader.onerror=reject;reader.readAsDataURL(file)});const r=await fetch('/api/content-assets',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({filename:file.name,contentType:file.type,dataBase64:data})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Image upload failed');return d.url};
  const save=async()=>{setBusy(true);setError('');try{await onSave({...form,blocks,html:blocksToHtml(blocks,brokers)},!document)}catch(e){setError(e instanceof Error?e.message:'Could not save')}finally{setBusy(false)}};
  return <div className="fixed inset-0 z-[130] flex items-center justify-center bg-ink-950/60 p-3"><div className="flex max-h-[96vh] w-full max-w-6xl flex-col overflow-hidden rounded-3xl bg-white shadow-soft-lg"><header className="flex items-center gap-3 bg-ink-950 px-5 py-4 text-white"><div className="min-w-0 flex-1"><p className="font-display text-lg font-bold">{document?'Edit':'Create'} Guide</p><p className="text-xs text-slate-400">Visual PageBuilder — the same editor used by Best-For and broker editorial content.</p></div><button onClick={onClose} className="rounded-lg p-2 text-slate-400 hover:bg-white/10">✕</button></header><main className="flex-1 overflow-y-auto p-5 sm:p-7"><div className="grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold text-slate-600">Title<input value={form.title||''} onChange={e=>setForm((f:any)=>({...f,title:e.target.value}))} className="mt-1.5 w-full rounded-xl border border-line px-3 py-2.5 text-sm"/></label><label className="text-xs font-bold text-slate-600">Slug<input value={form.slug||''} onChange={e=>setForm((f:any)=>({...f,slug:e.target.value}))} className="mt-1.5 w-full rounded-xl border border-line px-3 py-2.5 text-sm"/></label></div><label className="mt-4 block text-xs font-bold text-slate-600">Excerpt<textarea value={form.excerpt||''} onChange={e=>setForm((f:any)=>({...f,excerpt:e.target.value}))} rows={3} className="mt-1.5 w-full rounded-xl border border-line px-3 py-2.5 text-sm"/></label><div className="mt-6 rounded-2xl border border-line bg-paper p-4"><div className="flex items-center justify-between"><div><p className="font-display text-base font-bold text-ink-950">Page content</p><p className="text-xs text-slate-500">Add headings, text, images, tables, broker cards, comparison tables, CTAs and verdicts.</p></div></div><div className="mt-4 rounded-xl border border-line bg-white p-3"><PageBuilder value={blocks} onChange={next=>setBlocks(next)} onUploadImage={uploadImage}/></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold text-slate-600">SEO title<input value={form.seo_title||''} onChange={e=>setForm((f:any)=>({...f,seo_title:e.target.value}))} className="mt-1.5 w-full rounded-xl border border-line px-3 py-2.5 text-sm"/></label><label className="text-xs font-bold text-slate-600">SEO description<textarea value={form.seo_description||''} onChange={e=>setForm((f:any)=>({...f,seo_description:e.target.value}))} rows={2} className="mt-1.5 w-full rounded-xl border border-line px-3 py-2.5 text-sm"/></label></div><div className="mt-4 flex gap-3"><label className="flex flex-1 items-center justify-between rounded-xl border border-line bg-paper p-4 text-sm font-bold">Published<input type="checkbox" checked={!!form.published} onChange={e=>setForm((f:any)=>({...f,published:e.target.checked}))}/></label><label className="flex flex-1 items-center justify-between rounded-xl border border-line bg-paper p-4 text-sm font-bold">Indexable<input type="checkbox" checked={!!form.indexable} onChange={e=>setForm((f:any)=>({...f,indexable:e.target.checked}))}/></label></div></main><footer className="flex justify-end gap-2 border-t border-line px-5 py-4"><button onClick={onClose} className="rounded-xl border border-line px-4 py-2.5 text-xs font-bold">Cancel</button><button disabled={busy} onClick={save} className="rounded-xl bg-emerald-500 px-4 py-2.5 text-xs font-bold text-white">{busy?'Saving…':'Save guide'}</button></footer></div></div>;
}
