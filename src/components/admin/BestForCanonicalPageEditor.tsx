import { useEffect, useMemo, useState } from 'react';
import { Save, X } from 'lucide-react';
import type { Broker, ContentDocument, CountryPage } from '../../lib/types';
import { blocksToHtml, type PageBlock, type ComparisonField } from '../PageBuilder';
import BestForEditorialPageBuilder from '../BestForEditorialPageBuilder';
import ManualBrokerOrder from '../ManualBrokerOrder';
import { brokerMatchesTopic, getCountrySeoTopic, rankCountryTopicBrokers } from '../../data/countrySeoTopics';
import { fetchCountryBrokerAvailability, publicIntentSlug } from '../../lib/api';

type Kind = 'global' | 'country' | 'localized';
type Props = { kind: Kind; document: ContentDocument | null; brokers: Broker[]; countries: CountryPage[]; token: string; countrySlug?: string; topicSlug: string; locale?: string; onClose: () => void; onSave: (document: ContentDocument, isNew: boolean) => Promise<void> };
const MAX_MANUAL_BROKERS = 9;
const SECTION_COPY_KEYS = { ranking: 'bestfor-copy:ranking', comparison: 'bestfor-copy:comparison', brokerAnalysis: 'bestfor-copy:brokerAnalysis' } as const;
const DEFAULT_SECTION_COPY = {
  ranking: 'Explore the top eligible brokers for this category and jump directly to the full analysis of any broker that interests you.',
  comparison: 'Compare the eligible brokers side by side across the metrics that matter most for this category.',
  brokerAnalysis: "Review each broker's key facts, assessment, detailed analysis and PipRank verdict before you decide.",
};
const settingsOf = (doc: ContentDocument | null) => (doc?.settings ?? {}) as Record<string, any>;
const COMPARISON_FIELDS: { value: ComparisonField; label: string }[] = [
  { value: 'min_deposit', label: 'Minimum deposit' }, { value: 'spread_eurusd', label: 'EUR/USD spread' }, { value: 'commission', label: 'Commission' }, { value: 'max_leverage', label: 'Maximum leverage' }, { value: 'platforms', label: 'Platforms' }, { value: 'payments', label: 'Payment methods' }, { value: 'regulations', label: 'Regulation' },
];
const DEFAULT_COMPARISON_FIELDS: ComparisonField[] = ['min_deposit', 'spread_eurusd', 'commission', 'max_leverage'];
function cleanComparisonFields(value: unknown): ComparisonField[] { const allowed = new Set(COMPARISON_FIELDS.map((item) => item.value)); return Array.isArray(value) ? value.filter((item): item is ComparisonField => allowed.has(item as ComparisonField)) : []; }
function cleanPinned(value: unknown): string[] { return Array.isArray(value) ? Array.from(new Set(value.filter((item): item is string => typeof item === 'string' && item.trim().length > 0))).slice(0, MAX_MANUAL_BROKERS) : []; }
function normalizeSectionCopyBlocks(document: ContentDocument | null): PageBlock[] {
  const base = Array.isArray(document?.blocks) ? document!.blocks as PageBlock[] : [];
  const settings = settingsOf(document);
  const legacy = Array.isArray(settings.sections) ? settings.sections as { title?: string; html?: string }[] : [];
  const next = [...base];
  (Object.keys(SECTION_COPY_KEYS) as Array<keyof typeof SECTION_COPY_KEYS>).forEach((key) => {
    const id = SECTION_COPY_KEYS[key];
    if (next.some((block) => block.id === id)) return;
    const oldTitle = key === 'ranking' ? '__bestfor_ranking_description' : key === 'comparison' ? '__bestfor_comparison_description' : '__bestfor_broker_analysis_description';
    const legacyCopy = legacy.find((section) => section.title === oldTitle);
    if (legacyCopy?.html) next.push({ id, type: 'richtext', html: legacyCopy.html, editorialSection: 'introduction' });
  });
  return next;
}
function blank(kind: Kind, countrySlug?: string, topicSlug?: string, locale?: string): ContentDocument { const content_type = kind === 'global' ? 'global-best-for' : kind === 'country' ? 'country-best-for' : 'localized-best-for'; const content_key = kind === 'global' ? `best-for:${topicSlug || 'new'}` : kind === 'country' ? `country-best-for:${countrySlug || 'new'}:${topicSlug || 'new'}` : `localized-best-for:${countrySlug || 'new'}:${locale || 'en'}:${topicSlug || 'new'}`; return { id: 0, content_key, content_type, country_slug: countrySlug || null, topic_slug: topicSlug || null, slug: topicSlug || '', title: '', excerpt: '', html: '', blocks: [], seo_title: null, seo_description: null, indexable: true, published: false, updated_by: null, created_at: '', updated_at: '', settings: kind === 'localized' ? { locale: locale || 'en', comparisonFields: DEFAULT_COMPARISON_FIELDS } : { comparisonFields: DEFAULT_COMPARISON_FIELDS } }; }

export default function BestForCanonicalPageEditor({ kind, document, brokers, countries, token, countrySlug, topicSlug, locale, onClose, onSave }: Props) {
  const [form, setForm] = useState<ContentDocument>(() => document ? { ...document, settings: document.settings ?? {} } : blank(kind, countrySlug, topicSlug, locale));
  const [blocks, setBlocks] = useState<PageBlock[]>(() => normalizeSectionCopyBlocks(document));
  const [rankingMode, setRankingMode] = useState<'auto' | 'manual'>(() => settingsOf(document).rankingMode === 'manual' ? 'manual' : 'auto');
  const [pinned, setPinned] = useState<string[]>(() => cleanPinned(settingsOf(document).pinnedBrokerSlugs));
  const [comparisonFields, setComparisonFields] = useState<ComparisonField[]>(() => { const saved = cleanComparisonFields(settingsOf(document).comparisonFields); return saved.length ? saved : DEFAULT_COMPARISON_FIELDS; });
  const [countryAvailableSlugs, setCountryAvailableSlugs] = useState<string[] | null>(null);
  const [countryAvailabilityLoading, setCountryAvailabilityLoading] = useState(false);
  const [countryAvailabilityError, setCountryAvailabilityError] = useState('');
  const excluded = useMemo(() => Array.isArray(settingsOf(document).excludedBrokerSlugs) ? settingsOf(document).excludedBrokerSlugs : [], [document]);
  const [busy, setBusy] = useState(false); const [error, setError] = useState('');
  const [authors, setAuthors] = useState<ContentDocument[]>([]);
  useEffect(() => { fetch('/api/content-documents?type=author').then((r) => r.ok ? r.json() : []).then((rows) => setAuthors(Array.isArray(rows) ? rows : [])).catch(() => setAuthors([])); }, []);
  useEffect(() => { setForm(document ? { ...document, settings: document.settings ?? {} } : blank(kind, countrySlug, topicSlug, locale)); setBlocks(Array.isArray(document?.blocks) ? document!.blocks as PageBlock[] : []); const s = settingsOf(document); setRankingMode(s.rankingMode === 'manual' ? 'manual' : 'auto'); setPinned(cleanPinned(s.pinnedBrokerSlugs)); const saved = cleanComparisonFields(s.comparisonFields); setComparisonFields(saved.length ? saved : DEFAULT_COMPARISON_FIELDS); }, [document?.id, kind, countrySlug, topicSlug, locale]);
  useEffect(() => {
    let active = true;
    if ((kind !== 'country' && kind !== 'localized') || !countrySlug) { setCountryAvailableSlugs(null); setCountryAvailabilityError(''); setCountryAvailabilityLoading(false); return () => { active = false; }; }
    setCountryAvailabilityLoading(true); setCountryAvailabilityError('');
    fetchCountryBrokerAvailability(countrySlug)
      .then((rows) => { if (!active) return; const available = rows.filter((row) => row.status !== 'unavailable').map((row) => String(row.broker_id)); const brokerIds = new Map(brokers.map((broker) => [String(broker.id), broker.slug])); setCountryAvailableSlugs(Array.from(new Set(available.map((id) => brokerIds.get(id)).filter((slug): slug is string => Boolean(slug))))); })
      .catch((e) => { if (!active) return; setCountryAvailableSlugs(null); setCountryAvailabilityError(e instanceof Error ? e.message : 'Could not load country broker availability'); })
      .finally(() => { if (active) setCountryAvailabilityLoading(false); });
    return () => { active = false; };
  }, [brokers, countrySlug, kind]);
  const manualPool = useMemo(() => {
    const topic = String(form.topic_slug || topicSlug || '').trim().toLowerCase();
    const excludedSet = new Set(excluded);
    if (!topic) return [] as Broker[];
    if (kind === 'global') {
      return brokers.filter((broker) => !excludedSet.has(broker.slug));
    }
    if (kind === 'localized') {
      if (countryAvailableSlugs === null) return brokers.filter((broker) => !excludedSet.has(broker.slug));
      return brokers.filter((broker) => countryAvailableSlugs.includes(broker.slug) && !excludedSet.has(broker.slug));
    }
    const country = countries.find(c => c.slug === (form.country_slug || countrySlug));
    const countryTopic = getCountrySeoTopic(topic);
    if (!country || !countryTopic) return [] as Broker[];
    if (countryAvailableSlugs === null) return rankCountryTopicBrokers(brokers, country, countryTopic).filter(b => !excludedSet.has(b.slug));
    return brokers.filter((broker) => countryAvailableSlugs.includes(broker.slug) && !excludedSet.has(broker.slug));
  }, [brokers, countries, countryAvailableSlugs, countrySlug, excluded, form.country_slug, form.topic_slug, kind, topicSlug]);
  const analysisBrokers = useMemo(() => { const bySlug = new Map(manualPool.map((broker) => [broker.slug, broker])); const ordered = rankingMode === 'manual' ? pinned.map((slug) => bySlug.get(slug)).filter(Boolean) as Broker[] : manualPool; return [...ordered, ...manualPool.filter((broker) => !ordered.some((item) => item.slug === broker.slug))].slice(0, MAX_MANUAL_BROKERS); }, [manualPool, pinned, rankingMode]);
  const unavailablePinned = pinned.filter(slug => !manualPool.some(b => b.slug === slug));
  const uploadImage = async (file: File) => { const reader = new FileReader(); const data = await new Promise<string>((resolve, reject) => { reader.onload = () => resolve(String(reader.result)); reader.onerror = reject; reader.readAsDataURL(file); }); const res = await fetch('/api/content-assets', { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify({ filename: file.name, contentType: file.type, dataBase64: data }) }); const out = await res.json().catch(() => ({})); if (!res.ok) throw new Error(out.error || 'Image upload failed'); return out.url as string; };
  const save = async () => { try { setBusy(true); setError(''); if (rankingMode === 'manual' && pinned.length === 0) throw new Error('Manual ranking requires at least one selected broker.'); if (pinned.length > MAX_MANUAL_BROKERS) throw new Error(`Manual ranking supports a maximum of ${MAX_MANUAL_BROKERS} brokers.`); if (comparisonFields.length === 0) throw new Error('Select at least one comparison metric.'); const settings = (form.settings ?? {}) as Record<string, any>; const { sections: _legacySections, ...cleanSettings } = settings; const payload = { ...form, settings: { ...cleanSettings, ...(kind === 'localized' ? { locale: String(settings.locale || locale || '').trim().slice(0, 40) } : {}), rankingMode, pinnedBrokerSlugs: cleanPinned(pinned), excludedBrokerSlugs: Array.from(new Set(excluded)), comparisonFields: Array.from(new Set(comparisonFields)), label: String(settings.label || '').trim().slice(0, 500), author_slug: String(settings.author_slug || '').trim().toLowerCase(), reviewed_by_slug: String(settings.reviewed_by_slug || '').trim().toLowerCase() }, blocks, html: blocksToHtml(blocks, brokers) } as ContentDocument; await onSave(payload, !document); } catch (e) { setError(e instanceof Error ? e.message : 'Could not save Best-For page'); } finally { setBusy(false); } };
  const settings = (form.settings ?? {}) as Record<string, any>;
  const sectionCopy = {
    ranking: String(blocks.find((block) => block.id === SECTION_COPY_KEYS.ranking && block.type === 'richtext')?.html || DEFAULT_SECTION_COPY.ranking),
    comparison: String(blocks.find((block) => block.id === SECTION_COPY_KEYS.comparison && block.type === 'richtext')?.html || DEFAULT_SECTION_COPY.comparison),
    brokerAnalysis: String(blocks.find((block) => block.id === SECTION_COPY_KEYS.brokerAnalysis && block.type === 'richtext')?.html || DEFAULT_SECTION_COPY.brokerAnalysis),
  };
  const updateSectionCopy = (key: keyof typeof SECTION_COPY_KEYS, value: string) => {
    setBlocks((current) => {
      const id = SECTION_COPY_KEYS[key];
      const existing = current.find((block) => block.id === id);
      if (existing) return current.map((block) => block.id === id ? { ...block, type: 'richtext', html: value, editorialSection: 'introduction' } : block);
      return [...current, { id, type: 'richtext', html: value, editorialSection: 'introduction' }];
    });
  };
  const toggleComparisonField = (field: ComparisonField) => setComparisonFields(current => current.includes(field) ? current.filter(item => item !== field) : [...current, field]);
  return <div className="fixed inset-0 z-[130] flex items-center justify-center bg-ink-950/60 p-3 backdrop-blur-sm"><div className="flex max-h-[96vh] w-full max-w-7xl flex-col overflow-hidden rounded-3xl bg-white shadow-soft-lg"><header className="flex items-center gap-3 bg-ink-950 px-5 py-4 text-white"><div className="min-w-0 flex-1"><p className="font-display text-lg font-bold">{document ? 'Edit' : 'Create'} Best-For page</p><p className="text-xs text-slate-400">Structured hero, dynamic rankings, broker modules and PageBuilder editorial content.</p></div><button type="button" onClick={onClose} className="rounded-lg p-2 text-slate-400 hover:bg-white/10 hover:text-white"><X size={18} /></button></header><main className="flex-1 overflow-y-auto p-5 sm:p-7">{error && <p className="mb-5 rounded-xl bg-rose-50 px-4 py-3 text-sm text-rose-600">{error}</p>}<section className="rounded-2xl border border-line bg-paper p-4 sm:p-5"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-emerald-700">Hero</p><h3 className="mt-1 font-display text-lg font-bold text-ink-950">Structured hero content</h3><p className="mt-1 text-xs text-slate-500">The hero is template-controlled; edit the page identity and supporting fields here.</p><div className="mt-4 grid gap-4 sm:grid-cols-2"><label><span className="text-xs font-bold">Title / H1</span><input value={form.title || ''} onChange={e => setForm(f => ({ ...f, title: e.target.value }))} className="mt-1.5 w-full rounded-xl border border-line bg-white px-3 py-2.5 text-sm" /></label><label><span className="text-xs font-bold">Hero eyebrow</span><input value={String(settings.label || '')} onChange={e => setForm(f => ({ ...f, settings: { ...(f.settings || {}), label: e.target.value } }))} placeholder="PipRank Best For" className="mt-1.5 w-full rounded-xl border border-line bg-white px-3 py-2.5 text-sm" /></label></div><label className="mt-4 block"><span className="text-xs font-bold">Hero supporting text</span><textarea value={form.excerpt || ''} onChange={e => setForm(f => ({ ...f, excerpt: e.target.value }))} rows={3} className="mt-1.5 w-full rounded-xl border border-line bg-white px-3 py-2.5 text-sm" /></label></section>{kind === 'localized' && <label className="mt-4 block"><span className="text-xs font-bold">Locale</span><input value={String(form.settings?.locale || locale || '')} onChange={e => setForm(f => ({ ...f, settings: { ...(f.settings || {}), locale: e.target.value } }))} className="mt-1.5 w-full rounded-xl border border-line bg-white px-3 py-2.5 text-sm" /></label>}<div className="mt-6"><BestForEditorialPageBuilder value={blocks} onChange={setBlocks} onUploadImage={uploadImage} analysisBrokers={analysisBrokers} /></div><section className="mt-6 rounded-2xl border border-line bg-paper p-4"><div className="flex items-start justify-between gap-4"><div><p className="font-display text-sm font-bold">Broker ranking</p><p className="mt-1 text-xs text-slate-500">Automatic uses the existing rules. Manual chooses the exact eligible brokers and their order. The public page renders a maximum of nine eligible results.</p></div><span className="shrink-0 rounded-full bg-ink-950 px-2.5 py-1 text-[10px] font-black text-white">{Math.min(pinned.length, MAX_MANUAL_BROKERS)}/{MAX_MANUAL_BROKERS}</span></div>{(kind === 'country' || kind === 'localized') && <div className="mt-3 rounded-xl border border-blue-100 bg-blue-50 px-3 py-2 text-[11px] leading-5 text-blue-800">{countryAvailabilityLoading ? 'Checking broker availability for this country…' : countryAvailabilityError ? `Availability check unavailable: ${countryAvailabilityError}` : `${manualPool.length} eligible brokers available for this country and category.`}</div>}<select value={rankingMode} onChange={e => setRankingMode(e.target.value as 'auto' | 'manual')} className="mt-3 h-10 rounded-xl border border-line bg-white px-3 text-xs font-bold"><option value="auto">Automatic ranking</option><option value="manual">Manual broker order</option></select>{rankingMode === 'manual' && <div className="mt-4 max-w-xl"><ManualBrokerOrder brokers={manualPool} value={pinned} onChange={setPinned} />{unavailablePinned.length > 0 && <p className="mt-3 rounded-xl bg-amber-50 p-3 text-xs text-amber-800">{unavailablePinned.length} saved broker selection(s) are currently ineligible and will not render publicly.</p>}</div>}</section><section className="mt-6 rounded-2xl border border-line bg-paper p-4 sm:p-5"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-emerald-700">Attribution</p><label className="mt-2 block"><span className="text-xs font-bold">Written / reviewed by</span><select value={String(settings.reviewed_by_slug || settings.author_slug || '')} onChange={e => setForm(f => ({ ...f, settings: { ...(f.settings || {}), author_slug: e.target.value, reviewed_by_slug: e.target.value } }))} className="mt-1.5 h-10 w-full rounded-xl border border-line bg-white px-3 text-sm"><option value="">Use deterministic fallback</option>{authors.map(a => <option key={a.slug || a.id} value={a.slug || ''}>{a.title}</option>)}</select></label></section>
          <section className="mt-6 rounded-2xl border border-line bg-paper p-4 sm:p-5"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-emerald-700">Section copy</p><h3 className="mt-1 font-display text-lg font-bold text-ink-950">User-facing section descriptions</h3><p className="mt-1 max-w-3xl text-xs leading-5 text-slate-500">These descriptions appear on the public page. They are editable here and stay in the same canonical Best-For document.</p><div className="mt-4 grid gap-4"><label><span className="text-xs font-bold">Ranking description</span><textarea value={sectionCopy.ranking} onChange={e => updateSectionCopy('ranking', e.target.value)} rows={3} className="mt-1.5 w-full rounded-xl border border-line bg-white px-3 py-2.5 text-sm" /></label><label><span className="text-xs font-bold">Comparison description</span><textarea value={sectionCopy.comparison} onChange={e => updateSectionCopy('comparison', e.target.value)} rows={3} className="mt-1.5 w-full rounded-xl border border-line bg-white px-3 py-2.5 text-sm" /></label><label><span className="text-xs font-bold">Broker analysis description</span><textarea value={sectionCopy.brokerAnalysis} onChange={e => updateSectionCopy('brokerAnalysis', e.target.value)} rows={3} className="mt-1.5 w-full rounded-xl border border-line bg-white px-3 py-2.5 text-sm" /></label></div></section><section className="mt-6 rounded-2xl border border-line bg-paper p-4 sm:p-5"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-emerald-700">Quick comparison</p><h3 className="mt-1 font-display text-lg font-bold text-ink-950">Choose the metrics for this category</h3><p className="mt-1 max-w-2xl text-xs leading-5 text-slate-500">The comparison table uses the metrics selected here. PipRank Score remains the only user-visible score; rating and Trust Score are intentionally excluded.</p><div className="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{COMPARISON_FIELDS.map((field) => { const active = comparisonFields.includes(field.value); return <button key={field.value} type="button" onClick={() => toggleComparisonField(field.value)} className={`flex items-center justify-between rounded-xl border px-3 py-3 text-left text-sm font-bold transition ${active ? 'border-emerald-300 bg-emerald-50 text-emerald-800' : 'border-line bg-white text-slate-600 hover:border-slate-300'}`}><span>{field.label}</span><span className={`flex h-5 w-5 items-center justify-center rounded-full border text-[11px] ${active ? 'border-emerald-500 bg-emerald-600 text-white' : 'border-slate-300 text-transparent'}`}>✓</span></button>; })}</div></section><div className="mt-6 grid gap-4 sm:grid-cols-2"><label><span className="text-xs font-bold">SEO title</span><input value={form.seo_title || ''} onChange={e => setForm(f => ({ ...f, seo_title: e.target.value }))} className="mt-1.5 w-full rounded-xl border border-line bg-white px-3 py-2.5 text-sm" /></label><label><span className="text-xs font-bold">SEO description</span><textarea value={form.seo_description || ''} onChange={e => setForm(f => ({ ...f, seo_description: e.target.value }))} rows={2} className="mt-1.5 w-full rounded-xl border border-line bg-white px-3 py-2.5 text-sm" /></label></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="flex items-center justify-between rounded-xl border border-line bg-paper p-4"><span className="text-sm font-bold">Publish</span><input type="checkbox" checked={!!form.published} onChange={e => setForm(f => ({ ...f, published: e.target.checked }))} /></label><label className="flex items-center justify-between rounded-xl border border-line bg-paper p-4"><span className="text-sm font-bold">Index</span><input type="checkbox" checked={!!form.indexable} onChange={e => setForm(f => ({ ...f, indexable: e.target.checked }))} /></label></div></main><footer className="flex justify-end gap-2 border-t border-line px-5 py-4"><button type="button" onClick={onClose} className="rounded-xl border border-line px-4 py-2.5 text-xs font-bold">Cancel</button><button type="button" disabled={busy} onClick={save} className="inline-flex items-center gap-2 rounded-xl bg-ink-950 px-5 py-2.5 text-xs font-bold text-white"><Save size={14} />{busy ? 'Saving…' : 'Save Best-For page'}</button></footer></div></div>;
}