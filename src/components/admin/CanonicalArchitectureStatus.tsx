import type { Broker, ContentDocument, CountryPage, Intent } from '../../lib/types';
import { BEST_FOR_CANONICAL } from '../../lib/seo';

export default function CanonicalArchitectureStatus({ intents, countries, docs, brokers }: { intents:Intent[]; countries:CountryPage[]; docs:ContentDocument[]; brokers:Broker[] }) {
 const globals=Object.entries(BEST_FOR_CANONICAL); const globalDocs=globals.filter(([legacy])=>docs.some(d=>d.content_key===`best-for:${legacy}`)); const countryDocs=docs.filter(d=>d.content_type==='country-topic'&&d.country_slug&&d.topic_slug); const brokerDocs=docs.filter(d=>d.content_type==='broker');
 return <div className="rounded-2xl border border-line bg-white p-5"><p className="text-xs font-bold uppercase tracking-widest text-emerald-700">Architecture status</p><h3 className="mt-1 font-display text-xl font-bold text-ink-950">Canonical content ownership</h3><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Metric label="Global canonical" value={`${globalDocs.length}/${globals.length}`} sub="Best-For documents"/><Metric label="Country canonical" value={String(countryDocs.length)} sub="country-topic documents"/><Metric label="Broker canonical" value={`${brokerDocs.length}/${brokers.length}`} sub="broker main documents"/><Metric label="Countries" value={String(countries.length)} sub="available country hubs"/></div><div className="mt-4 rounded-xl bg-paper p-4 text-xs leading-5 text-slate-600"><b className="text-ink-950">Canonical global URLs:</b> {globals.map(([,slug])=>`/${slug}`).join(' · ')}<br/><span className="text-slate-400">Legacy /best/* URLs are redirects and are not content owners.</span></div></div>;
}
function Metric({label,value,sub}:{label:string;value:string;sub:string}){return <div className="rounded-xl border border-line bg-paper p-4"><p className="text-[10px] font-bold uppercase tracking-widest text-slate-400">{label}</p><p className="mt-1 font-display text-2xl font-bold text-ink-950">{value}</p><p className="text-xs text-slate-500">{sub}</p></div>}
